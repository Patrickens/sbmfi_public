# this is for maximum likelihood estimation using the Sequental Quadratic Programming approach
# We can compute gradient in least-squares loss from Jacobian J = dx/dv, grad = df(x)/dv = df(x)/dx * dx/dv
# This should replace 13CFLUX2